<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:components="clr-namespace:MoneyMate.Components"
    x:Class="MoneyMate.Views.BasePage"
    BackgroundColor="{StaticResource BackgroundLight}"
    Shell.NavBarIsVisible="False">

    <!-- ✅ Gestion des zones sûres (SafeArea) -->
    <Grid RowDefinitions="Auto,*,Auto"
          Padding="{OnPlatform iOS='0,40,0,0', Android='0', WinUI='0'}">

        <!-- 🔝 HEADER -->
        <components:HeaderAuthenticated Grid.Row="0" />

        <!-- 🧩 CONTENU CENTRAL -->
        <ContentPresenter x:Name="DynamicPresenter" Grid.Row="1" />

        <!-- 🔻 NAVBAR -->
        <components:Navbar Grid.Row="2" />
    </Grid>
</ContentPage>
